// import { PRODUCT_API_URL } from '../../apiConfig'; // chỉnh path đúng thư mục

// export const getProductAndImagesByCode = async (productCode: string) => {
//   const response = await fetch(`${PRODUCT_API_URL}/code/${productCode}/images`);
//   if (!response.ok) {
//     throw new Error('Failed to fetch product and images');
//   }
//   const result = await response.json();
//   return result.data;
// };
