// import { CLOUDINARY_BASE_URL } from "../services/apiConfig";

// export function buildCloudinaryUrl(imagePath?: string): string {
//   if (!imagePath) return "https://via.placeholder.com/120x120.png?text=No+Image";

//   // Nếu imagePath đã có http hoặc https → dùng nguyên
//   if (imagePath.startsWith("http")) return imagePath;

//   return `${CLOUDINARY_BASE_URL}/${imagePath}`;
// }
